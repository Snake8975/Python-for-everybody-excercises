# process of asking the name to the user
varPromptName = "Enter your name: \n"
varName = input(varPromptName)

# showing the name inserted
print(varName)

# letting the user reads what he typed
import time
time.sleep(2.5)